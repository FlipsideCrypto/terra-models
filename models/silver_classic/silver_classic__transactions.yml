version: 2
models:
  - name: silver_classic__transactions
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - BLOCK_ID
            - CHAIN_ID
            - TX_ID
