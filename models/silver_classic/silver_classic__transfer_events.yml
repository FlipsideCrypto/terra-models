version: 2
models:
  - name: silver_classic__transfer_events
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - TX_ID
            - MSG_INDEX
            - ACTION_INDEX
            - DENOM
            - AMOUNT