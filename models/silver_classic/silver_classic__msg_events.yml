version: 2
models:
  - name: silver_classic__msg_events
    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - BLOCK_ID
            - CHAIN_ID
            - EVENT_INDEX
            - EVENT_TYPE
            - MSG_INDEX
            - TX_ID
